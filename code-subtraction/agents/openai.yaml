interface:
  display_name: "Code Subtraction"
  short_description: "Safely simplify code by removing unnecessary complexity"
  default_prompt: "Use $code-subtraction to find and safely remove unnecessary complexity in my code, with ablation-based verification."
policy:
  allow_implicit_invocation: true
